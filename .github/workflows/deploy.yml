name: Deploy Elio → VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via cron VPS
        run: echo "✅ Push reçu — le cron job sur le VPS déploiera dans max 5 minutes."
